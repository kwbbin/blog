<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kwbbin.dao.UserDao">
    <sql id="Base_Column_List" >
        id,name,email,password,register_time,openType,openId,accessToken,expiredTime,nickname,avatar
    </sql>

    <select id="selectUserById" resultType="user">
        select * from user where id=#{id}
    </select>
    <select id="selectUserByOpenId" resultType="user">
        select * from user where openId=#{openId}
    </select>

    <select id="selectByNickNameAndPassword" parameterType="user" resultType="user">
        select
        <include refid="Base_Column_List"></include>
        from user
        where nickname=#{nickname} and password=#{password}
    </select>

    <insert id="insertUser" parameterType="user">
        insert into user(id,name,email,password,register_time,openType,openId,accessToken,expiredTime,nickname,avatar)
            values(#{id},#{name},#{email},#{password},#{register_time},#{openType},#{openId},#{accessToken},#{expiredTime},#{nickname},#{avatar})
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

</mapper>